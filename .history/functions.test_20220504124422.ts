const {shuffleArray} = require('./utils')

describe('shuffleArray should', () => {
         


  it('some text', () => {
    const a = [1,2,3,4,5];
    expect(shuffleArray(a)).toHaveLength(4)

})

  
  
});